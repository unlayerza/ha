import type {ClusterMessage,Transport,ChaosPolicy} from "./types";import {id} from "./id";
export class InMemoryTransport implements Transport{static peers=new Map<string,InMemoryTransport>();private handler?:((m:ClusterMessage)=>Promise<void>);policy:ChaosPolicy={};constructor(public address:string){}async start(h:(m:ClusterMessage)=>Promise<void>){this.handler=h;InMemoryTransport.peers.set(this.address,this)}async stop(){InMemoryTransport.peers.delete(this.address)}private async deliver(to:string,m:ClusterMessage){if(this.policy.partition?.has(to)||this.policy.partition?.has(m.from))return;if(this.policy.dropRate&&Math.random()<this.policy.dropRate)return;if(this.policy.delayMs)await Bun.sleep(this.policy.delayMs);await InMemoryTransport.peers.get(to)?.handler?.(m);if(this.policy.duplicateRate&&Math.random()<this.policy.duplicateRate)await InMemoryTransport.peers.get(to)?.handler?.({...m,id:id("dup")})}async send(to:string,m:ClusterMessage){await this.deliver(to,m)}async broadcast(from:string,m:ClusterMessage){await Promise.all([...InMemoryTransport.peers.keys()].filter(x=>x!==from).map(x=>this.deliver(x,m)))}} 
function wire(m:ClusterMessage){return JSON.stringify({...m,term:m.term.toString()})}function unwire(s:string){const m=JSON.parse(s) as any;m.term=BigInt(m.term);return m as ClusterMessage}
export class HttpTransport implements Transport{private server?:ReturnType<typeof Bun.serve>;private peers=new Map<string,string>();constructor(private address:string,private secret?:string){}setPeers(peers:Record<string,string>){this.peers=new Map(Object.entries(peers))}async start(handler:(m:ClusterMessage)=>Promise<void>){const [hostname,portText]=this.address.includes(":")?this.address.split(":"):["0.0.0.0",this.address];this.server=Bun.serve({hostname,port:Number(portText),fetch:async req=>{if(req.method!=="POST"||new URL(req.url).pathname!=="/ha/message")return new Response("not found",{status:404});try{await handler(unwire(await req.text()));return Response.json({ok:true})}catch{return Response.json({ok:false},{status:400})}}})}async stop(){this.server?.stop()}private endpoint(to:string){return (this.peers.get(to)||to).replace(/\/$/,"")+"/ha/message"}async send(to:string,m:ClusterMessage){const r=await fetch(this.endpoint(to),{method:"POST",headers:{"content-type":"application/json"},body:wire(m)});if(!r.ok)throw new Error("HA peer returned "+r.status)}async broadcast(from:string,m:ClusterMessage){await Promise.all([...this.peers.keys()].filter(x=>x!==from).map(x=>this.send(x,m)))}} 
export class ChaosTransport implements Transport{constructor(private inner:Transport,private policy:ChaosPolicy,private rand=()=>Math.random()){}async start(h:(m:ClusterMessage)=>Promise<void>){return this.inner.start?.(h)}async stop(){return this.inner.stop?.()}async send(to:string,m:ClusterMessage){if(this.policy.partition?.has(to)||this.policy.partition?.has(m.from))return;if(this.rand()<(this.policy.dropRate||0))return;if(this.policy.delayMs)await Bun.sleep(this.policy.delayMs);await this.inner.send(to,m);if(this.rand()<(this.policy.duplicateRate||0))await this.inner.send(to,{...m,id:id("dup")})}async broadcast(from:string,m:ClusterMessage){if(this.rand()<(this.policy.dropRate||0))return;await this.inner.broadcast(from,m)}}
