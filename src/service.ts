import type { ServiceAdapter, HAState, Authority, MembershipSnapshot } from "./types";
export class ServiceRegistry {private adapters=new Set<ServiceAdapter>(); add(a:ServiceAdapter){this.adapters.add(a);return()=>this.adapters.delete(a)} async authority(a:Authority|null){for(const x of this.adapters)await x.onAuthority?.(a)} async membership(s:MembershipSnapshot){for(const x of this.adapters)await x.onMembership?.(s)} async health(s:HAState){for(const x of this.adapters)await x.onHealth?.(s)} async fence(reason:string){for(const x of this.adapters)await x.onFence?.(reason)} async drain(){for(const x of this.adapters)await x.onDrain?.()} async recover(){for(const x of this.adapters)await x.onRecover?.()}}
export interface DatabaseAdapter {onHAAuthority(a:Authority|null):Promise<void>; onReplicationPosition?(position:unknown):Promise<void>; onFence?(token:string):Promise<void>}
export interface IdentityAdapter {registerNode(nodeId:string,address:string):Promise<void>; fenceMutations(term:bigint):Promise<void>}
export interface VoiceAdapter {onNodeHealth(nodeId:string,healthy:boolean):Promise<void>; onControlAuthority?(a:Authority|null):Promise<void>; onDrain?():Promise<void>}
