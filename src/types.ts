export type NodeStatus="provision"|"joining"|"syncing"|"healthy"|"degraded"|"draining"|"unhealthy"|"suspect"|"failed"|"removed"|"quarantined"|"retired";export type LifecycleState="provision"|"join"|"sync"|"ready"|"active"|"drain"|"recover"|"retired"|"quarantine";export type Role="follower"|"candidate"|"leader";export interface HANode{id:string;service:string;cluster:string;address:string;region?:string;zone?:string;version:string;protocolVersion:string;createdAt:number;status:NodeStatus;lifecycle:LifecycleState}export interface Member extends HANode{incarnation:number;lastSeen:number}export interface HAState{term:bigint;leaderId:string|null;quorum:boolean;role:Role;status:NodeStatus;lifecycle:LifecycleState;fenced:boolean;membershipRevision:bigint;membershipSize:number;votingSize:number;membershipReady:boolean}export interface Authority{term:bigint;nodeId:string;fencingToken:string;issuedAt:number;expiresAt:number}export interface MembershipSnapshot{version:bigint;members:Member[]}export interface QuorumState{membership:boolean;election:boolean;service:boolean;size:number;required:number}export interface Heartbeat{from:string;term:bigint;sentAt:number;role:Role;authorityToken?:string}export interface VoteRequest{from:string;term:bigint;lastTerm:bigint;lastIndex:number}export interface VoteResponse{from:string;term:bigint;granted:boolean;reason?:string}export interface ClusterMessage{id:string;kind:string;from:string;fromAddress?:string;term:bigint;sentAt:number;payload:unknown;signature:string}export interface HAEvent{id:string;type:string;at:number;nodeId:string;term?:bigint;correlationId?:string;data?:Record<string,unknown>}export interface ServiceAdapter{service:string;onAuthority?(authority:Authority|null):Promise<void>|void;onMembership?(snapshot:MembershipSnapshot):Promise<void>|void;onHealth?(state:HAState):Promise<void>|void;onFence?(reason:string):Promise<void>|void;onDrain?():Promise<void>|void;onRecover?():Promise<void>|void}export interface StateStore{load():Promise<Record<string,unknown>>;save(state:Record<string,unknown>):Promise<void>}export interface Clock{now():number;sleep(ms:number):Promise<void>}export interface Transport{send(to:string,message:ClusterMessage):Promise<void>;broadcast(from:string,message:ClusterMessage):Promise<void>;start?(handler:(message:ClusterMessage)=>Promise<void>):Promise<void>;stop?():Promise<void>}export interface ChaosPolicy{delayMs?:number;dropRate?:number;duplicateRate?:number;reorder?:boolean;seed?:number;partition?:Set<string>}