import type {Clock,Role,Transport,ClusterMessage,VoteRequest,VoteResponse,Member} from "./types";import {id} from "./id";import {StaleTermError} from "./errors";interface ElectionHooks{members():Member[];healthy():number;persist(term:bigint,votedFor:string|null):Promise<void>;onRole(role:Role,term:bigint):Promise<void>;onTerm(term:bigint):Promise<void>}export interface ElectionOptions{random?:()=>number}
export class Election{role:Role="follower";term=0n;leaderId:string|null=null;votedFor:string|null=null;private deadline=0;private timer?:Timer;private votes=new Set<string>();private lastHeartbeat=0;private random:()=>number;constructor(private nodeId:string,private transport:Transport,private clock:Clock,private heartbeatMs:number,private minElectionMs:number,private maxElectionMs:number,private hooks:ElectionHooks,o:ElectionOptions={}){this.random=o.random||Math.random}setNodeId(id:string){this.nodeId=id}private timeout(){const span=this.maxElectionMs-this.minElectionMs;return this.minElectionMs+(span?Math.floor(this.random()*span):0)}private resetDeadline(){this.deadline=this.clock.now()+this.timeout()}async restore(term:bigint,votedFor:string|null){this.term=term;this.votedFor=votedFor;this.resetDeadline()}async start(){this.resetDeadline();this.timer=setInterval(()=>void this.tick(),Math.max(20,Math.floor(this.heartbeatMs/2)))}async stop(){if(this.timer)clearInterval(this.timer)}private async tick(){if(this.role==="leader"){if(this.clock.now()-this.lastHeartbeat>=this.heartbeatMs){this.lastHeartbeat=this.clock.now();await this.broadcastHeartbeat()}return}if(this.clock.now()>=this.deadline)await this.startElection()}private async startElection(){this.role="candidate";this.term++;this.votedFor=this.nodeId;this.votes=new Set([this.nodeId]);await this.hooks.persist(this.term,this.votedFor);await this.hooks.onTerm(this.term);await this.hooks.onRole(this.role,this.term);this.resetDeadline();const req:VoteRequest={from:this.nodeId,term:this.term,lastTerm:this.term,lastIndex:0};await this.send("vote_request",req)}private async send(kind:string,payload:unknown){const m={id:id("msg"),kind,from:this.nodeId,term:this.term,sentAt:this.clock.now(),payload,signature:""} as ClusterMessage;await this.transport.broadcast(this.nodeId,m)}private async broadcastHeartbeat(){await this.send("heartbeat",{leaderId:this.nodeId,term:this.term})}async receive(m:ClusterMessage){if(m.term<this.term)throw new StaleTermError();if(m.term>this.term){this.term=m.term;this.role="follower";this.votedFor=null;this.leaderId=null;await this.hooks.persist(this.term,null);await this.hooks.onTerm(this.term);await this.hooks.onRole(this.role,this.term);this.resetDeadline()}if(m.kind==="heartbeat")return this.onHeartbeat(m);if(m.kind==="vote_request")return this.onVoteRequest(m);if(m.kind==="vote_response")return this.onVoteResponse(m)}private async onHeartbeat(m:ClusterMessage){this.leaderId=(m.payload as any).leaderId||m.from;this.lastHeartbeat=this.clock.now();this.resetDeadline();if(this.role!=="follower"){this.role="follower";await this.hooks.onRole(this.role,this.term)}}private async onVoteRequest(m:ClusterMessage){const r=m.payload as VoteRequest;let grant=false;if(r.term===this.term&&(this.votedFor===null||this.votedFor===r.from)){grant=true;this.votedFor=r.from;await this.hooks.persist(this.term,this.votedFor);this.resetDeadline()}const resp:VoteResponse={from:this.nodeId,term:this.term,granted:grant,reason:grant?undefined:"already_voted"};const out={id:id("msg"),kind:"vote_response",from:this.nodeId,fromAddress:undefined,term:this.term,sentAt:this.clock.now(),payload:resp,signature:""} as ClusterMessage;await this.transport.send(m.fromAddress||m.from,out)}private async onVoteResponse(m:ClusterMessage){const r=m.payload as VoteResponse;if(r.term!==this.term||this.role!=="candidate"||!r.granted)return;this.votes.add(r.from);if(this.votes.size>=Math.floor(this.hooks.members().length/2)+1){this.role="leader";this.leaderId=this.nodeId;this.lastHeartbeat=0;await this.hooks.onRole(this.role,this.term);await this.broadcastHeartbeat()}}}