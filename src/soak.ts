import {LocalProcessCluster} from "./process-harness";import {ChaosController} from "./chaos";
const hours=Number(Bun.env.HA_SOAK_HOURS||0);const duration=Number(Bun.env.HA_SOAK_MS||(hours?hours*3600000:3600000));const seed=Number(Bun.env.CHAOS_SEED||12345);const chaos=new ChaosController(seed);const cluster=new LocalProcessCluster(Number(Bun.env.HA_SOAK_NODES||3));await cluster.start();const end=Date.now()+duration;try{while(Date.now()<end){const action=chaos.choose([{type:"kill",node:"0"},{type:"kill",node:"1"},{type:"kill",node:"2"},{type:"delay",ms:100},{type:"reorder"}]);if(action.type==="kill"){const i=Number(action.node);await cluster.hardKill(i);await Bun.sleep(1000);await cluster.restart(i)}else await Bun.sleep(action.ms||250);for(let i=0;i<cluster.nodes.length;i++){try{const s=await cluster.state(i) as any;chaos.recordInvariant("node-"+i+"-has-state",!!s.term)}catch{chaos.recordInvariant("node-"+i+"-reachable",false)}}}console.log(JSON.stringify(chaos.finish(),null,2))}finally{await cluster.cleanup()}}
